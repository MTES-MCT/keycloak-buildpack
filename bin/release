#!/usr/bin/env bash

cat << EOF
---
addons:
 - scalingo-postgresql
config_vars:
  PATH: "/app/bin:/app/keycloak/bin:/app/java/bin:/usr/local/bin:/usr/bin:/bin"
default_process_types:
  web: /app/keycloak/bin/kc.sh --verbose start --hostname-port $PORT
EOF